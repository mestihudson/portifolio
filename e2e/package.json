{
  "license": "MIT",
  "scripts": {
    "cucumber": "cucumber-js --require cucumber.conf.js --format node_modules/cucumber-pretty --require steps --require-module babel-core/register",
    "test:chrome": "browser=chrome npm run cucumber",
    "test:firefox": "browser=firefox npm run cucumber",
    "test": "npm run test:chrome && npm run test:firefox"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.2.0",
    "cucumber": "^5.1.0",
    "cucumber-pretty": "^1.5.0",
    "string": "^3.3.3",
    "nightwatch": "^1.2.1",
    "nightwatch-api": "^2.3.0"
  }
}
